/**
 * All of the JS code 
 */

(function( $ ) {
 
	// Add Color Picker to all inputs that have 'color-field' class
	$(function() {
	  $('.miqpa-color-field').wpColorPicker();
	});
     
})( jQuery );